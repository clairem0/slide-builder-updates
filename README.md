# Slide Builder Updates

This public repository contains only the current version metadata used by the
private Slide Builder alpha app. It contains no application source or
installers. Access to alpha downloads is managed separately.
